---
title: "GNOCHI: Generative Neural mOdel for Close Human-Human Interactions"
title_zh: GNOCHI：面向紧密人际交互的生成式神经模型
authors: "Gonzalo Gómez-Nogales, Marc Comino-Trinidad, Andrés Casado-Elvira, Dan Casas"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10408v1"
tags: ["query:d-artic-kin"]
score: 8.0
evidence: 使用cVAE生成了用于近距离交互的3D人体姿态（运动学参数）
tldr: 生成紧密3D人-人交互因自由度大且需避免碰撞而困难，现有方法缺乏生成能力。本文提出基于条件变分自编码器（cVAE）的生成模型GNOCHI，通过自动数据增强生成合成交互数据，并利用自监督碰撞损失确保物理正确性。实验表明，该方法能生成多样、逼真且物理正确的紧密交互，优于现有最先进方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10408v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1427, \"height\": 568, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 880, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1788, \"height\": 1114, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1767, \"height\": 1737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10408v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 801, \"height\": 158, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10408v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1682, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10408v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 502, \"height\": 236, \"label\": \"Table\"}]"
motivation: 现有生成方法难以处理紧密人机交互中的碰撞和多样性，需要能生成物理正确且可控的交互模型。
method: 采用条件变分自编码器，以一人姿态为条件生成另一人姿态；自动数据增强生成合成交互数据；自监督碰撞损失确保无穿透。
result: 模型能生成多种逼真且物理正确的紧密交互，在多样性和碰撞避免上超越当前最先进方法。
conclusion: GNOCHI有效解决了紧密人机交互中的数据稀缺和碰撞问题，实现了高质量合成。
---

## 摘要
在虚拟环境中创建逼真的三维人际交互极具挑战性，因为人体具有高自由度，且需要生成彼此不碰撞的物理精确姿态。传统人际交互方法依赖于运动捕捉或三维人体重建，但缺乏生成能力。最近的生成式方法可通过文本或图像输入合成个体或交互运动，但在建模紧密交互方面普遍不足。本文提出一种新颖的生成模型，用于紧密三维人际交互，采用条件变分自编码器（cVAE），该模型基于一个人的姿态生成另一个人的姿态，从而实现可控且多样化的交互合成。为训练模型，我们解决了人际交互领域两个长期存在的根本性挑战：数据稀缺问题，对此我们提出一种自动监督数据增强策略，生成合成但逼真的交互姿态；生成方法中的碰撞感知问题，对此我们提出一种基于碰撞解决技术的自监督损失，利用体积代理确保物理正确的交互。我们广泛评估了模型的性能，并展示了多种合理且物理正确的交互，这些交互无法通过当前最先进方法生成。

## Abstract
Creating realistic 3D human-human interactions in virtual environments is challenging due to the high degrees of freedom in the human body and the need for physically accurate poses that do not collide with each other. Traditional methods for human-human interaction are based on motion tracking or 3D body reconstruction, but lack generative capabilities. Recent generative methods enable the synthesis of individual or interacting motions via text or image input, but generally fall short in modeling close interactions. This paper introduces a novel generative model for close 3D human-human interactions using a conditional variational autoencoder (cVAE), which generates poses for one human conditioned on the pose of another, allowing for controlled and diverse interaction synthesis. To train our model, we address two underlying long-standing challenges in the field of human-human interaction: data scarcity, for which we propose an automated supervised data augmentation strategy that generates synthetic yet realistic interaction poses; and collision awareness in generative approaches, for which we propose a self-supervised loss based on a collision resolution technique using volumetric proxies to ensure physically correct interactions. We extensively evaluate the capabilities of our model, and demonstrate a wide variety of plausible and physically correct interactions, not possible to generate with current state-of-the-art methods.

---

## 论文详细总结（自动生成）

# 论文详细总结：GNOCHI: Generative Neural mOdel for Close Human-Human Interactions

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在虚拟环境中创建真实的三维人际紧密交互（如拥抱、舞蹈、格斗等）非常困难，因为人体具有高自由度，且需要生成物理上准确、不会发生碰撞（mesh interpenetration）的姿态。现有方法（如基于运动捕捉或3D重建）缺乏生成能力，而最近的生成式方法（如扩散模型）虽能合成个体或交互运动，但难以处理紧密交互中的碰撞和多样性，且缺乏可控性。
- **研究背景**：人际交互研究分为人类-场景、人类-物体和人类-人类三类。本文聚焦人类-人类模型中的紧密交互，现有工作多关注跟踪/重建而非生成。BUDDI是唯一生成式例外，但它学习联合隐空间，条件采样时易导致根位置/朝向错误或未解决的碰撞。本文提出显式姿态条件生成模型GNOCHI，旨在生成物理正确、多样、可控的紧密交互姿态，并服务于动画创作、数据集增强和图像生成控制等应用。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用条件变分自编码器（cVAE），以条件人体的SMPL姿态参数为输入，生成反应人体的姿态参数（包括关节旋转、全局旋转和相对平移）。为了训练，解决两大难题：数据稀缺和生成过程中的碰撞。分别采用自动数据增强策略和自监督碰撞损失。
- **关键技术细节**：
  - **姿态表示**：采用SMPL参数，每个姿态表示为 `p = {θ, r, t}`，其中θ为23个关节旋转（用连续6D表示），r为全局旋转（3×2），t为全局平移（3D）。所有姿态相对于条件人体坐标系。
  - **条件变分自编码器（cVAE）**：编码器E输入 `{θ_C, θ_R, t_R, r_R}`，输出隐变量z的均值μ和方差σ；解码器D输入 `{θ_C, z}`，输出反应姿态 `{θ̂_R, r̂_R, t̂_R}`。使用共享和专用MLP结构。损失函数为KL散度项和SMPL网格顶点重建损失（L1距离）。
  - **数据增强（Section 3）**：
    - 个体层面姿态增强：对每个关节施加随机旋转噪声（轴角表示，各关节有不同权重和最大角度），然后通过VPoser投影到可行姿态子空间，保证解剖学合理性。
    - 人-人碰撞解决：使用基于SDF的物理模拟器（OpenVDB）计算每个顶点对其他人体表面的穿透距离，对穿透顶点施加惩罚力 `E_coll(δ) = 0.5 * k * δ^2`，沿最近表面法向方向。采用后向欧拉积分，牛顿法一步迭代，求解稀疏线性系统（6×24×2）。最终生成无碰撞的合成交互数据。
  - **碰撞感知模块（CF）**：cVAE训练后，再训练一个MLP模块CF，输入条件姿态和cVAE预测的反应姿态（包括旋转、平移），输出关节旋转偏移量Δθ̂_R，使调整后的反应姿态不与条件姿态碰撞。采用自监督训练：用VPoser随机采样条件姿态，cVAE生成反应姿态，CF输出偏移。损失函数包括：碰撞损失L_col（基于24个胶囊刚体之间的距离矩阵，计算低于容忍阈值DT的负距离惩罚），偏移正则化L_Δθ̂，关节位置正则化L_joints。
  - **推理流程**：给定条件姿态θ_C，采样z~N(0,1)，通过解码器和CF模块得到最终反应姿态：`p_R = CF(D({θ_C, z}))`。

## 3. 实验设计

- **使用的数据集**：
  - **源数据**：Hi4D数据集（包含4D人体交互实例分割），从中选取包含接触交互（mesh间距<1cm）的帧，共计5744帧原始数据。
  - **增强数据**：对每对姿态生成10个变体，经碰撞解决后滤除不再接触的样本，最终得到55897个增强样本；合并原始数据后共61641个姿态。
  - **测试集**：采用序列保留分割（sequence-heldout），即同一序列的所有帧分配到同一分割，防止信息泄漏。训练集49202帧，测试集12439帧。
- **基准与对比方法**：
  - **对比方法**：BUDDI（扩散模型，通过in-painting策略实现条件生成）。
  - **自身消融**：四种模型变体：(1) 基线cVAE（仅L_rec+L_KL，无增强）；(2) 基线cVAE+增强；(3) cVAE+增强+融入L_col损失；(4) 本文完整方法（cVAE+增强+CF模块）。
- **评估场景**：
  - **In-Distribution (ID)**：从Hi4D测试集选取100个条件姿态，每个生成10个反应，共1000个样本，计算重建误差（L_rec, E_rR, E_tR）、碰撞指标（Inter体积、IoU）、VPoser合理性分数。
  - **Out-of-Distribution (OOD)**：从Mixamo动画选取35个动画的70个关键帧，共700个样本，只计算碰撞和合理性指标。
  - **用户研究**：35名参与者对10组交互（每组包含GT、增强样本、生成样本三种条件）进行5分Likert评分，采用TOST等价检验。
  - **定性比较**：展示隐空间插值、条件采样多样性对比、BUDDI对比。

## 4. 资源与算力

- 论文中未明确提及GPU型号、数量或训练时长。仅提到使用Adam优化器，学习率1e-4，batch size 50。cVAE和CF模块的训练细节（如迭代次数）未给出。因此无法确定具体算力需求。

## 5. 实验数量与充分性

- **实验数量**：
  - 消融实验：4组变体对比，在ID和OOD下共8组指标计算。
  - 与BUDDI定量对比：1组（Intersection和IoU）。
  - 定性对比：多组（隐空间插值2例，条件采样3类互动：篮球、击掌、舞蹈）。
  - 用户研究：35人，10组交互×3条件=30个场景，随机分块。
- **充分性与公平性**：
  - **优点**：进行了ID和OOD评估，覆盖了分布内和分布外泛化；消融实验逐步验证了增强、碰撞损失、CF模块的作用；与BUDDI对比时采用了BUDDI生成的姿态作为条件，避免数据集偏差；用户研究验证了感知等效性。
  - **不足**：与BUDDI的对比仅在碰撞指标上，未对比重建精度或多样性指标（如FID）；缺乏与更多基线（如直接优化方法、其他生成模型）的对比；超参数（如λ_col=30, λ_joints=0.5等）未进行灵敏度分析；用户研究中样本量有限（35人），且仅评分静态姿态，未涉及动态序列。

## 6. 论文的主要结论与发现

- 提出的GNOCHI模型能够生成多样、物理正确、语义一致的紧密人-人交互姿态，优于BUDDI。
- 自动数据增强策略显著提高了模型性能（第二行对比第一行，所有指标大幅下降）。
- 直接加入碰撞损失L_col虽然能最小化碰撞，但严重损害重建质量和合理性（VPoser分数变差）。
- CF模块在保持重建精度和合理性的同时有效减少碰撞，实现了最好的综合权衡。
- 用户研究证实生成姿态在感知上与真实数据等价（p<0.0001）。
- 模型可应用于动画辅助创作（作为姿态copilot）和作为图像生成模型（如FLUX）的精细控制信号。

## 7. 优点

- **创新性**：首个显式姿态条件生成模型用于紧密交互，结合自动数据增强和自监督碰撞解决。
- **数据增强策略**：利用VPoser保证个体姿态自然性，SDF物理模拟保证交互无碰撞，生成大量合成样本，降低了对昂贵运动捕捉数据的依赖。
- **碰撞感知模块（CF）**：以自监督方式训练，无需碰撞标注数据；胶囊代理快速计算距离，容忍阈值DT从训练数据统计确定，平衡接触与穿透。
- **隐空间性质**：插值平滑，控制灵活，适用于动画创作。
- **全面评估**：包含定性和定量、ID和OOD、用户研究、与SOTA对比，证据充分。
- **应用价值**：可作为后处理工具改进现有跟踪重建结果，并为图像/视频生成提供精确姿态控制。

## 8. 不足与局限

- **静态姿态局限**：仅处理静态姿态，未建模时间动态。论文承认联系路径常定义语义，未来需扩展到时间依赖（如动态交互动画）。
- **无连续碰撞检测**：增强时仅用静态SDF解决穿透，未采用连续碰撞检测（CCD），可能丢失首次接触细节（尤其手指等细薄结构）。
- **缺乏语义约束**：随机增强可能产生社交上不合理的姿态（如亲密接触过度），但用户研究未发现此类问题，需进一步探索。
- **CF模块局限**：仅为几何预处理，非完整物理模拟，不考虑肌肉变形、软体接触或重力平衡；极端纠缠可能收敛到局部最小值；未包含自身碰撞处理（自穿透），尽管训练数据中已解决。
- **实验覆盖不足**：与BUDDI对比仅限碰撞指标，未评估其他维度（如多样性、编辑性）；无更广泛基线（如基于优化方法、其他cVAE变体）；超参数未进行网格搜索或灵敏度分析。
- **算力信息缺失**：未报告训练所需GPU型号、数量和时间，影响可复现性评估。
- **数据集偏差**：仅使用Hi4D子集和Mixamo OOD，未在其他交互数据集（如CHI3D, Inter-X）上验证泛化性。
- **用户研究局限**：样本量中等（35人），仅静态姿态评分，未涉及动态或交互任务。

（完）
