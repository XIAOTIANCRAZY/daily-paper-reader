---
title: Synchronized Three-Dimensional Vocal-Tract Motion for Speech Synchronization via Joint-Embedding Predictive Architecture Alignment
title_zh: 通过联合嵌入预测架构对齐实现同步的三维声道运动用于语音同步
authors: "Sheng Li, Takahiro Shinozaki"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11772v1"
tags: ["query:d-artic-kin"]
score: 8.0
evidence: 三维声道铰接模型的运动学参数
tldr: "现代神经语音系统虽能生成高质量波形，但缺乏可解释的发音运动。本文提出基于联合嵌入预测架构(JEPA)的对齐方法，将三维声道模型中的发音动作（如下颌、舌、唇）与保时长声学载波同步，并通过强化学习/交叉熵方法(RL/CEM)优化轨迹的物理合理性。在24个最小对词汇上，声学载波取得8.33%的词错误率和4.17%的字符错误率，音色保持得分为0.947，UTMOS得分为3.174，平均JEPA对齐得分为0.864。该方法首次实现了同步三维发音运动与高保真语音生成，平衡了声学质量与物理可解释性。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11772v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11772v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 826, \"height\": 278, \"label\": \"Table\"}]"
motivation: 填补可解释发音合成与高保真声学质量之间的空白，实现物理合理的同步三维声道运动。
method: 结合JEPA表示对齐发音动作与声学载波，并用RL/CEM优化轨迹以满足物理约束。
result: "24词集上WER 8.33%，CER 4.17%，UTMOS 3.174，JEPA得分0.864，音色保护0.947。"
conclusion: 成功同步三维发音运动与语音，兼顾物理合理性与声学质量。
---

## 摘要
现代神经语音系统可以生成可理解的波形，但它们通常隐藏了产生声音的物理语音生产状态。相反，生物力学声道模型揭示了发音结构、接触行为、气流路径以及几何约束，但直接的物理波形合成仍不如现代神经声码器稳健。一个持续时间的声学载体提供了听觉波形，而一个校正后的三维声道模型提供了同步的下颌、嘴唇、舌头、软腭、喉部、口腔气流和鼻腔气流运动。一个联合嵌入预测架构（JEPA）风格的表示和一个强化学习/交叉熵方法（RL/CEM）轨迹选择循环将发音动作与声学载体以及物理合理性约束对齐。评估包含12个三维记录，覆盖24个最小对立对刺激。在24个单词集上，载体获得了良好的自动语音识别（ASR）结果（词错误率8.33%，字符错误率4.17%），UTMOS评分为3.174，平均JEPA评分为0.864，平均音色保护评分为0.947。

## Abstract
Modern neural speech systems can generate intelligible waveforms, but they usually hide the physical speech-production state that produced the sound. Conversely, biomechanical vocal-tract models expose articulatory structure, contact behavior, airflow routing, and geometric constraints, but direct physical waveform synthesis remains less robust than modern neural vocoders. A duration-preserving acoustic carrier supplies the listening waveform, while a corrected three-dimensional vocal-tract model supplies synchronized jaw, lip, tongue, velum, laryngeal, oral-airflow, and nasal-airflow motion. A joint-embedding predictive architecture (JEPA)-style representation and a reinforcement learning/cross-entropy method (RL/CEM) trajectory-selection loop align articulatory actions to the acoustic carrier and to physical-plausibility constraints. The evaluation contains 12 3D recordings covering 24 minimal-pair stimuli. On the 24-word set, the carrier obtains good automatic speech recognition (ASR) results (an 8.33\% WER, a 4.17\% CER), a UTMOS score of 3.174, a mean JEPA score of 0.864, and a mean timbre-guard score of 0.947.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

现代神经语音系统（如 Neural TTS、神经声码器）能生成高度可理解的波形，却隐藏了产生声音的物理语音生产状态（如颌、舌、唇、软腭等发音器官的运动）。反之，生物力学三维声道模型能直观呈现发音结构、接触行为、气流路径等可解释信息，但直接基于物理模拟合成波形在鲁棒性和自然度上远不如神经声码器。  
本文旨在填补这一空白：**在保持高保真听感的同时，提供同步、可检查的三维发音运动序列**，使语音系统兼具声学质量与发音可解释性。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：将“保时长声学载波”（来自参考语音或神经 TTS）作为最终听觉输出，将“校正的三维声道模型”产生的发音动作序列与载波对齐，使发音运动在时序、内容、物理合理性上都与载波匹配。
- **关键技术细节**：
  - **发音动作生成**：控制器 `πθ` 根据文本 `x`、音素序列 `p` 和目标时长 `d` 生成时间索引的动作序列 `a1:T`，包含颌、唇、舌、软腭、喉部、口腔气流、鼻腔气流等可解释控制量。
  - **三维声道模拟**：状态更新方程 `s_{t+1} = Fϕ(s_t, a_t, c_t)`，`c_t` 包含时序、平滑度、接触、可视性等约束。
  - **声学载波**：`yC = V(x, d)` 由外部参考或神经 TTS 生成，且保持时长不变。
  - **物理诊断路径**：`yphys = H(G(s_{1:T}), f0, e)` 仅为可选的物理合成分支，不作为最终输出。
  - **JEPA 对齐**：编码器 `Eω` 将观测 `o_t`（含动作、网格状态、气流标记、载波声学特征等）映射为潜在表示 `z_t`；预测器 `Pψ` 根据当前潜在上下文和未来动作预测未来潜在状态 `ẑ_{t+k}`；损失函数 `LJEPA = Σ||ẑ_{t+k} - sg(z_{t+k})||_1`，提供致密的对齐信号。
  - **RL/CEM 轨迹选择**：多目标奖励函数 `R = 0.64R_ASR + 0.16R_JEPA + 0.12R_UTMOS + 0.08R_timbre`，通过交叉熵方法挑选最优动作轨迹，确保载波内容可恢复、潜在表示一致、音频质量不退化、音色不漂移。
- **算法流程**：文本→音素/时长→发音动作生成→三维模拟产生状态序列→JEPA 预测与损失计算→RL/CEM 优化动作→与保时长声学载波合并→输出同步视音频。

## 3. 实验设计：数据集、benchmark 与对比方法

- **数据集**：作者自行录制了 12 个三维发音运动视频，覆盖 **24 个单词**，组成 **12 个最小对立对**（如 pat/bat, ten/den, cap/gap, sip/zip, fan/van, map/nap, light/right, beat/bit, bad/bed, cot/caught, pool/pull, woo/you）。
- **Benchmark**：无外部基准数据集。评估指标包括：
  - 自动语音识别（ASR）准确率（词错误率 WER、字符错误率 CER）
  - 语音质量自动评估（UTMOS）
  - 音色保持得分（timbre-guard score，基于平均基频 f0 和频谱质心变化）
  - JEPA 对齐得分
  - 组合奖励值
- **对比方法**：论文未将本系统与任何现有方法（如纯物理合成、纯神经声码器、其他发音合成系统）进行定量比较。属于系统自身能力诊断型实验。

## 4. 资源与算力

**文中未明确说明**实验所用的 GPU 型号、数量、训练时长、显存消耗等算力信息。仅提到使用 OpenGL 录制三维动画，以及 JEPA 和 RL/CEM 的优化过程，但未提供硬件配置细节。

## 5. 实验数量与充分性

- **实验组数**：仅一组 **24 个单词**（12 个最小对）的评测。未进行消融实验（如去除 JEPA 项、改变奖励权重、不同载波来源对比）、句子级或多说话人实验、跨语种测试等。
- **充分性与公平性**：论文自述当前评估是“诊断性的”（diagnostic）而非“语料库规模”。实验数量非常有限，不足以支撑通用性结论。且 UTMOS 和 ASR 均为自动代理指标，缺乏人工听感与发音专家评分。因此**实验不够充分，也未能证明与基线方法的公平对比**。

## 6. 论文的主要结论与发现

- 在 24 词集上，声学载波恢复率为 22/24 单词准确，平均 WER 8.33%、CER 4.17%，说明主听觉信号可理解性良好。
- 平均 UTMOS 3.174（自动估计，非人工 MOS）。
- 音色操作使频谱质心降低 8.83%，而基频几乎不变（119.54→119.06 Hz），音色保护得分 0.947。
- 平均 JEPA 得分 0.864，组合奖励 0.883。
- 系统成功生成了同步的三维发音视频，支持对双唇闭合、齿唇收紧、舌腭接触、鼻腔气流路径等发音细节的视觉检查。

## 7. 优点

- **方法创新**：首次将**联合嵌入预测架构（JEPA）** 用于发音动作与声学载波的对齐，提供致密预测损失以替代不可靠的物理波形目标。
- **多目标优化**：RL/CEM 机制同时考虑 ASR 内容恢复、潜在空间一致性、音频质量和音色保护，避免单一指标导致的畸变。
- **物理可解释性**：三维声道模型展示解剖结构（皮肤、骨骼、舌、软腭、鼻腔等），并加入气流代理标记，使发音过程的几何与运动透明。
- **最小对协议**：设计了 12 个常见发音对比的最小对，为未来人工或自动评估提供了可审计的测试集。

## 8. 不足与局限

- **声学载波非物理合成**：最终听觉信号来自外部 TTS/参考，而非三维模型的物理声学求解器，因此系统不能提供完全物理一致的语音生成。
- **JEPA 并非物理理解**：对齐基于潜在表示，不保证模型真正理解了发音的流体力学或声学机制。
- **实验规模极小**：仅 24 个单词、单说话人、单语种，缺乏句子级、多说话人、多语种评估，结果外推性不足。
- **缺乏对比基线**：未与现有发音合成系统（如 VocalTractLab、MRI-to-Speech 方法）比较 WER、MOS 等指标，无法判断相对优劣。
- **自动指标可靠性**：UTMOS 和 ASR 为自动代理，未辅以人工听感测试、发音专家评分、唇读同步主观判断。
- **未报告算力需求**：无法评估方法在实际部署中的计算代价。
- **消融研究缺失**：未分析 JEPA 项、RL 权重、音色保护等模块的单独贡献，难以确认各组件有效性。

（完）
